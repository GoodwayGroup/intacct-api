machine:
    node:
        version: 4
dependencies:
    pre:
        - "npm config -g set progress=false"
test:
    pre:
        - "npm run lint"
    post:
        - "cp -r coverage/* $CIRCLE_TEST_REPORTS/"
